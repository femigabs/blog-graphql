{
  "watch": ["app"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "ts-node --files -r tsconfig-paths/register ./app/index.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}